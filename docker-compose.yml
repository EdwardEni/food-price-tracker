services:
  api:
    build:
      context: .  # Build from root
      dockerfile: api/Dockerfile  # Path to Dockerfile
    # ... rest unchanged

  dashboard:
    build:
      context: .  # Build from root
      dockerfile: dashboard/Dockerfile  # Path to Dockerfile
    # ... rest unchanged